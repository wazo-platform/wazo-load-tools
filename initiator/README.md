# Initiator

## wazo-setup

Initialize wazo server by creating default resources
The script will generate a configuration file used by users generators

```shell
./wazo-setup.py -s <wazo-ip> -p <password> -o ../generator/users/config.yml
```
